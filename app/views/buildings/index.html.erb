<h1>All Buildings</h1>
<ul>
<% @buildings.each do |building| %>
  <li><%= link_to building.name, building %></li>
  <% end %>
</ul>
<%= button_to "Add New Building", new_building_path, method: :get %>
